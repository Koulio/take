    /**
     * Method generated for query $query
     * @param source 
     * @return an iterator 
     * code generated using velocity template $templatename
    */
    private ResourceIterator< $resulttype > $methodname (
        final $sourcetype source, 
        final $targettype target, 
        final DerivationController _derivation,source,target) {
        
        _derivation.log($property,DerivationController.BEAN_PROPERTY);
        
         final java.util.Iterator<$targettype> iterator =  source.$accessor ().iterator();
         while (iterator.hasNext()) {
         	if (target.equals(iterator.next()) {
         	    $resulttype result = new $resulttype ();
        		result. $slot1 = source;
        		result. $slot2 = target;
        		return new SingletonIterator<$resulttype>(result); 
         	}
         }
        return EmptyIterator.DEFAULT;
    }